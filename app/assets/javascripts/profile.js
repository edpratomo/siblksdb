$(function() {
  $('.gravatar_tip').poshytip({content: 'Jika belum punya gravatar, join gravatar.com untuk membuat'});
});
$(document).on('page:change', function() {
  $('.gravatar_tip').poshytip({content: 'Jika belum punya gravatar, join gravatar.com untuk membuat'});
});
