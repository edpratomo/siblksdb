<%= form_for(@instructors_schedule) do |f| %>
  <% if @instructors_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instructors_schedule.errors.count, "error") %> prohibited this instructors_schedule from being saved:</h2>

      <ul>
      <% @instructors_schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :schedule_id %><br>
    <%= f.number_field :schedule_id %>
  </div>
  <div class="field">
    <%= f.label :instructor_id %><br>
    <%= f.number_field :instructor_id %>
  </div>
  <div class="field">
    <%= f.label :day %><br>
    <%= f.text_area :day %>
  </div>
  <div class="field">
    <%= f.label :avail_seat %><br>
    <%= f.number_field :avail_seat %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
